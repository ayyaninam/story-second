// This page is only to keep it in-sync with the other icons - it doesn't really need to be a separate file.

import React from 'react';
import { FlipVertical } from 'lucide-react';

function TrendsOrientation() {
  return <FlipVertical size={20} />;
}

export default TrendsOrientation;